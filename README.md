# vk-chat-resume

Userscript для выгрузки сообщений из бесед VK (`vk.ru`) и создания краткого резюме с помощью Qwen Cloud.

## Возможности

- загрузка выбранного количества последних сообщений;
- загрузка сообщений за выбранный период в днях;
- копирование выгруженных сообщений в буфер обмена;
- два режима резюме: «Все события» и «Только важные»;
- ручное копирование резюме и исходных сообщений кнопками в окне результата;
- отображение периода, за который были выбраны сообщения;
- автоматическая проверка новых версий скрипта.

Кнопка `✨ Резюме` появляется только при открытой беседе VK.

## Установка

1. Установите менеджер userscript:
	- [Tampermonkey](https://www.tampermonkey.net/);
	- [Violentmonkey](https://violentmonkey.github.io/).
2. Откройте файл [vk-chat-resume.user.js](vk-chat-resume.user.js) в GitHub и нажмите **Raw**. Менеджер userscript предложит установить скрипт.
3. Подтвердите установку.
4. Перезагрузите страницу VK.

Для установки напрямую используйте [Raw-ссылку на скрипт](https://raw.githubusercontent.com/i-3Dmax/vk.ru-chat-resume/main/vk-chat-resume.user.js).

## Использование

1. Откройте VK и перейдите в нужную беседу. Поддерживаются адреса вида `/im/convo/123`.
2. Нажмите `✨ Резюме`.
3. Выберите способ загрузки:
	- последние сообщения и их количество;
	- сообщения за период в днях.
4. Дождитесь окончания загрузки (загружаются только текстовые данные).
5. Выберите тип резюме:
	- **Все события** — основные моменты переписки с активными участниками;
	- **Только важные** — только важные моменты без перечисления всех событий.
6. После обработки результат появится в отдельном окне.

В окне результата доступны кнопки ручного копирования резюме, копирования исходных сообщений и закрытия окна. Автоматическое копирование не выполняется.

## API-ключ Qwen Cloud

Для первого создания резюме потребуется API-ключ Qwen Cloud. Скрипт отправляет запрос к Qwen Cloud через разрешенный userscript API.

- зарегистрируйтесь на сайте qwencloud.com
- перейдите на страницу https://home.qwencloud.com/api-keys
- создайте и скопируйте API ключ (После закрытия окна с ключом, скопировать его уже будет не возможно. При необходимости, нужно будет создать новый ключ)
- после успешного запроса ключ сохраняется в cookie текущего домена VK на один год;
- при следующих запусках ключ вводить не нужно;
- при ответе Qwen с ошибкой авторизации `401` или `403` старый ключ удаляется и запрашивается новый.

## Анонимизация

Перед отправкой переписки в Qwen скрипт заменяет имена авторов на псевдонимы вида `User0001`, `User0002` и так далее. Это помогает скрыть имена участников во время обработки текста ИИ.

- одинаковый автор получает один и тот же псевдоним в рамках одного запроса;
- анонимизируются имена авторов обычных сообщений и ответов на сообщения;
- после ответа Qwen псевдонимы заменяются обратно на исходные имена;
- ручное копирование исходных сообщений сохраняет оригинальные имена;
- текст сообщений и созданное резюме отправляются в Qwen Cloud для обработки.
- ни названия чатов, ни какие другие данные в Qwen не передаются, только обезличенные тексты сообщений.